<nav class="nav" id="nav">
  <a href="/" class="nav-logo">
    {% include "logo.njk" %}
    Signature Therapeutic Wellness
  </a>
  <div class="nav-links">
    <div class="nav-drop">
      <a>Our Services <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg></a>
      <div class="nav-drop-menu">
        {% for item in site.nav %}{% if item.label == "Our Services" %}{% for child in item.children %}
        <a href="{{ child.href }}">{{ child.label }}</a>
        {% endfor %}{% endif %}{% endfor %}
      </div>
    </div>
    <div class="nav-drop">
      <a>Conditions We Treat <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg></a>
      <div class="nav-drop-menu">
        {% for item in site.nav %}{% if item.label == "Conditions We Treat" %}{% for child in item.children %}
        <a href="{{ child.href }}">{{ child.label }}</a>
        {% endfor %}{% endif %}{% endfor %}
      </div>
    </div>
    <a href="/insurance-accepted-massage/">Insurance</a>
    <a href="/price/">Price</a>
  </div>
  <div class="nav-cta">
    <a href="{{ site.giftCardUrl }}" class="gift-link" target="_blank" rel="noopener">Gift Cards</a>
    <a href="{{ site.bookUrl }}" class="btn nav-btn" target="_blank" rel="noopener">Book Now</a>
  </div>
  <button class="nav-toggle" id="nav-toggle" aria-label="Open menu" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
</nav>

<div class="mobile-menu" id="mobile-menu" role="dialog" aria-label="Navigation">
  <a href="/">Home</a>
  <div class="mobile-section">
    <div class="mobile-section-label">Our Services</div>
    {% for item in site.nav %}{% if item.label == "Our Services" %}{% for child in item.children %}
    <a href="{{ child.href }}">{{ child.label }}</a>
    {% endfor %}{% endif %}{% endfor %}
  </div>
  <div class="mobile-section">
    <div class="mobile-section-label">Conditions We Treat</div>
    {% for item in site.nav %}{% if item.label == "Conditions We Treat" %}{% for child in item.children %}
    <a href="{{ child.href }}">{{ child.label }}</a>
    {% endfor %}{% endif %}{% endfor %}
  </div>
  <div class="mobile-section">
    <div class="mobile-section-label">More</div>
    <a href="/insurance-accepted-massage/">Insurance Accepted</a>
    <a href="/price/">Price</a>
    <a href="/privacy-policy/">Privacy Policy</a>
    <a href="/accessibility-statement/">Accessibility Statement</a>
  </div>
  <div class="mobile-ctas">
    <a href="{{ site.bookUrl }}" class="btn" target="_blank" rel="noopener">Book Appointment</a>
    <a href="{{ site.giftCardUrl }}" class="btn-outline" target="_blank" rel="noopener">Gift Cards</a>
  </div>
</div>
